//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SUBMIT_TASK_RESULT_DISCRIMINATOR: [u8; 8] = [39, 108, 74, 4, 66, 125, 157, 7];

/// Accounts.
#[derive(Debug)]
pub struct SubmitTaskResult {
    pub payer: solana_pubkey::Pubkey,

    pub compute_node: solana_pubkey::Pubkey,

    pub task_data: solana_pubkey::Pubkey,

    pub agent: solana_pubkey::Pubkey,

    pub system_program: solana_pubkey::Pubkey,
}

impl SubmitTaskResult {
    pub fn instruction(
        &self,
        args: SubmitTaskResultInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SubmitTaskResultInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.push(solana_instruction::AccountMeta::new(
            self.compute_node,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.task_data, false));
        accounts.push(solana_instruction::AccountMeta::new(self.agent, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SubmitTaskResultInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::DAC_MANAGER_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SubmitTaskResultInstructionData {
    discriminator: [u8; 8],
}

impl SubmitTaskResultInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [39, 108, 74, 4, 66, 125, 157, 7],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SubmitTaskResultInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SubmitTaskResultInstructionArgs {
    pub result: Vec<u8>,
}

impl SubmitTaskResultInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SubmitTaskResult`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` payer
///   1. `[writable, signer]` compute_node
///   2. `[writable]` task_data
///   3. `[writable]` agent
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct SubmitTaskResultBuilder {
    payer: Option<solana_pubkey::Pubkey>,
    compute_node: Option<solana_pubkey::Pubkey>,
    task_data: Option<solana_pubkey::Pubkey>,
    agent: Option<solana_pubkey::Pubkey>,
    system_program: Option<solana_pubkey::Pubkey>,
    result: Option<Vec<u8>>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SubmitTaskResultBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_pubkey::Pubkey) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn compute_node(&mut self, compute_node: solana_pubkey::Pubkey) -> &mut Self {
        self.compute_node = Some(compute_node);
        self
    }
    #[inline(always)]
    pub fn task_data(&mut self, task_data: solana_pubkey::Pubkey) -> &mut Self {
        self.task_data = Some(task_data);
        self
    }
    #[inline(always)]
    pub fn agent(&mut self, agent: solana_pubkey::Pubkey) -> &mut Self {
        self.agent = Some(agent);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn result(&mut self, result: Vec<u8>) -> &mut Self {
        self.result = Some(result);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SubmitTaskResult {
            payer: self.payer.expect("payer is not set"),
            compute_node: self.compute_node.expect("compute_node is not set"),
            task_data: self.task_data.expect("task_data is not set"),
            agent: self.agent.expect("agent is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
        };
        let args = SubmitTaskResultInstructionArgs {
            result: self.result.clone().expect("result is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `submit_task_result` CPI accounts.
pub struct SubmitTaskResultCpiAccounts<'a, 'b> {
    pub payer: &'b solana_account_info::AccountInfo<'a>,

    pub compute_node: &'b solana_account_info::AccountInfo<'a>,

    pub task_data: &'b solana_account_info::AccountInfo<'a>,

    pub agent: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `submit_task_result` CPI instruction.
pub struct SubmitTaskResultCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub payer: &'b solana_account_info::AccountInfo<'a>,

    pub compute_node: &'b solana_account_info::AccountInfo<'a>,

    pub task_data: &'b solana_account_info::AccountInfo<'a>,

    pub agent: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SubmitTaskResultInstructionArgs,
}

impl<'a, 'b> SubmitTaskResultCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SubmitTaskResultCpiAccounts<'a, 'b>,
        args: SubmitTaskResultInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            payer: accounts.payer,
            compute_node: accounts.compute_node,
            task_data: accounts.task_data,
            agent: accounts.agent,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.compute_node.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.task_data.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.agent.key, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = SubmitTaskResultInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::DAC_MANAGER_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.compute_node.clone());
        account_infos.push(self.task_data.clone());
        account_infos.push(self.agent.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SubmitTaskResult` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` payer
///   1. `[writable, signer]` compute_node
///   2. `[writable]` task_data
///   3. `[writable]` agent
///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct SubmitTaskResultCpiBuilder<'a, 'b> {
    instruction: Box<SubmitTaskResultCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SubmitTaskResultCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SubmitTaskResultCpiBuilderInstruction {
            __program: program,
            payer: None,
            compute_node: None,
            task_data: None,
            agent: None,
            system_program: None,
            result: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn compute_node(
        &mut self,
        compute_node: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.compute_node = Some(compute_node);
        self
    }
    #[inline(always)]
    pub fn task_data(&mut self, task_data: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.task_data = Some(task_data);
        self
    }
    #[inline(always)]
    pub fn agent(&mut self, agent: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.agent = Some(agent);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn result(&mut self, result: Vec<u8>) -> &mut Self {
        self.instruction.result = Some(result);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SubmitTaskResultInstructionArgs {
            result: self.instruction.result.clone().expect("result is not set"),
        };
        let instruction = SubmitTaskResultCpi {
            __program: self.instruction.__program,

            payer: self.instruction.payer.expect("payer is not set"),

            compute_node: self
                .instruction
                .compute_node
                .expect("compute_node is not set"),

            task_data: self.instruction.task_data.expect("task_data is not set"),

            agent: self.instruction.agent.expect("agent is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SubmitTaskResultCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    compute_node: Option<&'b solana_account_info::AccountInfo<'a>>,
    task_data: Option<&'b solana_account_info::AccountInfo<'a>>,
    agent: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    result: Option<Vec<u8>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
