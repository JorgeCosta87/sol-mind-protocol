/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { NFT_OPERATIONS_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_TRADE_HUB_DISCRIMINATOR = new Uint8Array([
  29, 87, 149, 77, 65, 31, 30, 49,
]);

export function getCreateTradeHubDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_TRADE_HUB_DISCRIMINATOR
  );
}

export type CreateTradeHubInstruction<
  TProgram extends string = typeof NFT_OPERATIONS_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTradeHub extends string | AccountMeta<string> = string,
  TAccountProjectConfig extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTradeHub extends string
        ? WritableAccount<TAccountTradeHub>
        : TAccountTradeHub,
      TAccountProjectConfig extends string
        ? ReadonlyAccount<TAccountProjectConfig>
        : TAccountProjectConfig,
      TAccountProtocolConfig extends string
        ? WritableAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateTradeHubInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  feeBps: bigint;
};

export type CreateTradeHubInstructionDataArgs = {
  name: string;
  feeBps: number | bigint;
};

export function getCreateTradeHubInstructionDataEncoder(): Encoder<CreateTradeHubInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['feeBps', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_TRADE_HUB_DISCRIMINATOR })
  );
}

export function getCreateTradeHubInstructionDataDecoder(): Decoder<CreateTradeHubInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['feeBps', getU64Decoder()],
  ]);
}

export function getCreateTradeHubInstructionDataCodec(): Codec<
  CreateTradeHubInstructionDataArgs,
  CreateTradeHubInstructionData
> {
  return combineCodec(
    getCreateTradeHubInstructionDataEncoder(),
    getCreateTradeHubInstructionDataDecoder()
  );
}

export type CreateTradeHubAsyncInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountTradeHub extends string = string,
  TAccountProjectConfig extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  authority: TransactionSigner<TAccountAuthority>;
  tradeHub?: Address<TAccountTradeHub>;
  projectConfig: Address<TAccountProjectConfig>;
  protocolConfig?: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateTradeHubInstructionDataArgs['name'];
  feeBps: CreateTradeHubInstructionDataArgs['feeBps'];
};

export async function getCreateTradeHubInstructionAsync<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountTradeHub extends string,
  TAccountProjectConfig extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NFT_OPERATIONS_PROGRAM_ADDRESS,
>(
  input: CreateTradeHubAsyncInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountTradeHub,
    TAccountProjectConfig,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateTradeHubInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountTradeHub,
    TAccountProjectConfig,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? NFT_OPERATIONS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    tradeHub: { value: input.tradeHub ?? null, isWritable: true },
    projectConfig: { value: input.projectConfig ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tradeHub.value) {
    accounts.tradeHub.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 97, 100, 101, 95, 104, 117, 98])
        ),
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()).encode(
          expectSome(args.name)
        ),
        getAddressEncoder().encode(expectAddress(accounts.projectConfig.value)),
      ],
    });
  }
  if (!accounts.protocolConfig.value) {
    accounts.protocolConfig.value = await getProgramDerivedAddress({
      programAddress:
        'EkK8DLYGgXKi1Hcp5xpoyrkgMqxE6MqyhQh35QFACJ24' as Address<'EkK8DLYGgXKi1Hcp5xpoyrkgMqxE6MqyhQh35QFACJ24'>,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            115, 111, 108, 45, 109, 105, 110, 100, 45, 112, 114, 111, 116, 111,
            99, 111, 108,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tradeHub),
      getAccountMeta(accounts.projectConfig),
      getAccountMeta(accounts.protocolConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateTradeHubInstructionDataEncoder().encode(
      args as CreateTradeHubInstructionDataArgs
    ),
    programAddress,
  } as CreateTradeHubInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountTradeHub,
    TAccountProjectConfig,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type CreateTradeHubInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountTradeHub extends string = string,
  TAccountProjectConfig extends string = string,
  TAccountProtocolConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  authority: TransactionSigner<TAccountAuthority>;
  tradeHub: Address<TAccountTradeHub>;
  projectConfig: Address<TAccountProjectConfig>;
  protocolConfig: Address<TAccountProtocolConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateTradeHubInstructionDataArgs['name'];
  feeBps: CreateTradeHubInstructionDataArgs['feeBps'];
};

export function getCreateTradeHubInstruction<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountTradeHub extends string,
  TAccountProjectConfig extends string,
  TAccountProtocolConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NFT_OPERATIONS_PROGRAM_ADDRESS,
>(
  input: CreateTradeHubInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountTradeHub,
    TAccountProjectConfig,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateTradeHubInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAuthority,
  TAccountTradeHub,
  TAccountProjectConfig,
  TAccountProtocolConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? NFT_OPERATIONS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    tradeHub: { value: input.tradeHub ?? null, isWritable: true },
    projectConfig: { value: input.projectConfig ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tradeHub),
      getAccountMeta(accounts.projectConfig),
      getAccountMeta(accounts.protocolConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateTradeHubInstructionDataEncoder().encode(
      args as CreateTradeHubInstructionDataArgs
    ),
    programAddress,
  } as CreateTradeHubInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountTradeHub,
    TAccountProjectConfig,
    TAccountProtocolConfig,
    TAccountSystemProgram
  >);
}

export type ParsedCreateTradeHubInstruction<
  TProgram extends string = typeof NFT_OPERATIONS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    authority: TAccountMetas[1];
    tradeHub: TAccountMetas[2];
    projectConfig: TAccountMetas[3];
    protocolConfig: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CreateTradeHubInstructionData;
};

export function parseCreateTradeHubInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateTradeHubInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      authority: getNextAccount(),
      tradeHub: getNextAccount(),
      projectConfig: getNextAccount(),
      protocolConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateTradeHubInstructionDataDecoder().decode(instruction.data),
  };
}
