/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { DAC_MANAGER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLAIM_COMPUTE_NODE_DISCRIMINATOR = new Uint8Array([
  202, 199, 87, 60, 125, 64, 171, 108,
]);

export function getClaimComputeNodeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_COMPUTE_NODE_DISCRIMINATOR
  );
}

export type ClaimComputeNodeInstruction<
  TProgram extends string = typeof DAC_MANAGER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountComputeNode extends string | AccountMeta<string> = string,
  TAccountComputeNodeInfo extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountComputeNode extends string
        ? WritableSignerAccount<TAccountComputeNode> &
            AccountSignerMeta<TAccountComputeNode>
        : TAccountComputeNode,
      TAccountComputeNodeInfo extends string
        ? WritableAccount<TAccountComputeNodeInfo>
        : TAccountComputeNodeInfo,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimComputeNodeInstructionData = {
  discriminator: ReadonlyUint8Array;
  nodeInfoCid: string;
};

export type ClaimComputeNodeInstructionDataArgs = { nodeInfoCid: string };

export function getClaimComputeNodeInstructionDataEncoder(): Encoder<ClaimComputeNodeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['nodeInfoCid', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_COMPUTE_NODE_DISCRIMINATOR })
  );
}

export function getClaimComputeNodeInstructionDataDecoder(): Decoder<ClaimComputeNodeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['nodeInfoCid', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getClaimComputeNodeInstructionDataCodec(): Codec<
  ClaimComputeNodeInstructionDataArgs,
  ClaimComputeNodeInstructionData
> {
  return combineCodec(
    getClaimComputeNodeInstructionDataEncoder(),
    getClaimComputeNodeInstructionDataDecoder()
  );
}

export type ClaimComputeNodeInput<
  TAccountPayer extends string = string,
  TAccountComputeNode extends string = string,
  TAccountComputeNodeInfo extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  computeNode: TransactionSigner<TAccountComputeNode>;
  computeNodeInfo: Address<TAccountComputeNodeInfo>;
  systemProgram?: Address<TAccountSystemProgram>;
  nodeInfoCid: ClaimComputeNodeInstructionDataArgs['nodeInfoCid'];
};

export function getClaimComputeNodeInstruction<
  TAccountPayer extends string,
  TAccountComputeNode extends string,
  TAccountComputeNodeInfo extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_MANAGER_PROGRAM_ADDRESS,
>(
  input: ClaimComputeNodeInput<
    TAccountPayer,
    TAccountComputeNode,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimComputeNodeInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountComputeNode,
  TAccountComputeNodeInfo,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_MANAGER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    computeNode: { value: input.computeNode ?? null, isWritable: true },
    computeNodeInfo: { value: input.computeNodeInfo ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.computeNode),
      getAccountMeta(accounts.computeNodeInfo),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimComputeNodeInstructionDataEncoder().encode(
      args as ClaimComputeNodeInstructionDataArgs
    ),
    programAddress,
  } as ClaimComputeNodeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeNode,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >);
}

export type ParsedClaimComputeNodeInstruction<
  TProgram extends string = typeof DAC_MANAGER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    computeNode: TAccountMetas[1];
    computeNodeInfo: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ClaimComputeNodeInstructionData;
};

export function parseClaimComputeNodeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimComputeNodeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      computeNode: getNextAccount(),
      computeNodeInfo: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimComputeNodeInstructionDataDecoder().decode(instruction.data),
  };
}
