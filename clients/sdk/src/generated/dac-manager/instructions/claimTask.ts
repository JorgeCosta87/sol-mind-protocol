/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { DAC_MANAGER_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLAIM_TASK_DISCRIMINATOR = new Uint8Array([
  49, 222, 219, 238, 155, 68, 221, 136,
]);

export function getClaimTaskDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_TASK_DISCRIMINATOR);
}

export type ClaimTaskInstruction<
  TProgram extends string = typeof DAC_MANAGER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountComputeNode extends string | AccountMeta<string> = string,
  TAccountTaskData extends string | AccountMeta<string> = string,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountComputeNode extends string
        ? WritableSignerAccount<TAccountComputeNode> &
            AccountSignerMeta<TAccountComputeNode>
        : TAccountComputeNode,
      TAccountTaskData extends string
        ? WritableAccount<TAccountTaskData>
        : TAccountTaskData,
      TAccountAgent extends string
        ? WritableAccount<TAccountAgent>
        : TAccountAgent,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimTaskInstructionData = { discriminator: ReadonlyUint8Array };

export type ClaimTaskInstructionDataArgs = {};

export function getClaimTaskInstructionDataEncoder(): FixedSizeEncoder<ClaimTaskInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_TASK_DISCRIMINATOR })
  );
}

export function getClaimTaskInstructionDataDecoder(): FixedSizeDecoder<ClaimTaskInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimTaskInstructionDataCodec(): FixedSizeCodec<
  ClaimTaskInstructionDataArgs,
  ClaimTaskInstructionData
> {
  return combineCodec(
    getClaimTaskInstructionDataEncoder(),
    getClaimTaskInstructionDataDecoder()
  );
}

export type ClaimTaskAsyncInput<
  TAccountPayer extends string = string,
  TAccountComputeNode extends string = string,
  TAccountTaskData extends string = string,
  TAccountAgent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  computeNode: TransactionSigner<TAccountComputeNode>;
  taskData?: Address<TAccountTaskData>;
  agent: Address<TAccountAgent>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getClaimTaskInstructionAsync<
  TAccountPayer extends string,
  TAccountComputeNode extends string,
  TAccountTaskData extends string,
  TAccountAgent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_MANAGER_PROGRAM_ADDRESS,
>(
  input: ClaimTaskAsyncInput<
    TAccountPayer,
    TAccountComputeNode,
    TAccountTaskData,
    TAccountAgent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClaimTaskInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeNode,
    TAccountTaskData,
    TAccountAgent,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_MANAGER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    computeNode: { value: input.computeNode ?? null, isWritable: true },
    taskData: { value: input.taskData ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.taskData.value) {
    accounts.taskData.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 97, 115, 107, 95, 100, 97, 116, 97])
        ),
        getAddressEncoder().encode(expectAddress(accounts.agent.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.computeNode),
      getAccountMeta(accounts.taskData),
      getAccountMeta(accounts.agent),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimTaskInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimTaskInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeNode,
    TAccountTaskData,
    TAccountAgent,
    TAccountSystemProgram
  >);
}

export type ClaimTaskInput<
  TAccountPayer extends string = string,
  TAccountComputeNode extends string = string,
  TAccountTaskData extends string = string,
  TAccountAgent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  computeNode: TransactionSigner<TAccountComputeNode>;
  taskData: Address<TAccountTaskData>;
  agent: Address<TAccountAgent>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClaimTaskInstruction<
  TAccountPayer extends string,
  TAccountComputeNode extends string,
  TAccountTaskData extends string,
  TAccountAgent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_MANAGER_PROGRAM_ADDRESS,
>(
  input: ClaimTaskInput<
    TAccountPayer,
    TAccountComputeNode,
    TAccountTaskData,
    TAccountAgent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimTaskInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountComputeNode,
  TAccountTaskData,
  TAccountAgent,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_MANAGER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    computeNode: { value: input.computeNode ?? null, isWritable: true },
    taskData: { value: input.taskData ?? null, isWritable: true },
    agent: { value: input.agent ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.computeNode),
      getAccountMeta(accounts.taskData),
      getAccountMeta(accounts.agent),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimTaskInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimTaskInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeNode,
    TAccountTaskData,
    TAccountAgent,
    TAccountSystemProgram
  >);
}

export type ParsedClaimTaskInstruction<
  TProgram extends string = typeof DAC_MANAGER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    computeNode: TAccountMetas[1];
    taskData: TAccountMetas[2];
    agent: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ClaimTaskInstructionData;
};

export function parseClaimTaskInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimTaskInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      computeNode: getNextAccount(),
      taskData: getNextAccount(),
      agent: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimTaskInstructionDataDecoder().decode(instruction.data),
  };
}
