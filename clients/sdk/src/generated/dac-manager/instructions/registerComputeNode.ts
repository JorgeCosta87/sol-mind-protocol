/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { DAC_MANAGER_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const REGISTER_COMPUTE_NODE_DISCRIMINATOR = new Uint8Array([
  31, 230, 46, 205, 112, 221, 25, 55,
]);

export function getRegisterComputeNodeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_COMPUTE_NODE_DISCRIMINATOR
  );
}

export type RegisterComputeNodeInstruction<
  TProgram extends string = typeof DAC_MANAGER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountComputeNodeInfo extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountComputeNodeInfo extends string
        ? WritableAccount<TAccountComputeNodeInfo>
        : TAccountComputeNodeInfo,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterComputeNodeInstructionData = {
  discriminator: ReadonlyUint8Array;
  nodePubkey: Address;
};

export type RegisterComputeNodeInstructionDataArgs = { nodePubkey: Address };

export function getRegisterComputeNodeInstructionDataEncoder(): FixedSizeEncoder<RegisterComputeNodeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['nodePubkey', getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REGISTER_COMPUTE_NODE_DISCRIMINATOR,
    })
  );
}

export function getRegisterComputeNodeInstructionDataDecoder(): FixedSizeDecoder<RegisterComputeNodeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['nodePubkey', getAddressDecoder()],
  ]);
}

export function getRegisterComputeNodeInstructionDataCodec(): FixedSizeCodec<
  RegisterComputeNodeInstructionDataArgs,
  RegisterComputeNodeInstructionData
> {
  return combineCodec(
    getRegisterComputeNodeInstructionDataEncoder(),
    getRegisterComputeNodeInstructionDataDecoder()
  );
}

export type RegisterComputeNodeAsyncInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountComputeNodeInfo extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  owner: TransactionSigner<TAccountOwner>;
  computeNodeInfo?: Address<TAccountComputeNodeInfo>;
  systemProgram?: Address<TAccountSystemProgram>;
  nodePubkey: RegisterComputeNodeInstructionDataArgs['nodePubkey'];
};

export async function getRegisterComputeNodeInstructionAsync<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountComputeNodeInfo extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_MANAGER_PROGRAM_ADDRESS,
>(
  input: RegisterComputeNodeAsyncInput<
    TAccountPayer,
    TAccountOwner,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RegisterComputeNodeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_MANAGER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    computeNodeInfo: { value: input.computeNodeInfo ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.computeNodeInfo.value) {
    accounts.computeNodeInfo.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            99, 111, 109, 112, 117, 116, 101, 95, 110, 111, 100, 101,
          ])
        ),
        getAddressEncoder().encode(expectSome(args.nodePubkey)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.computeNodeInfo),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterComputeNodeInstructionDataEncoder().encode(
      args as RegisterComputeNodeInstructionDataArgs
    ),
    programAddress,
  } as RegisterComputeNodeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >);
}

export type RegisterComputeNodeInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountComputeNodeInfo extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  owner: TransactionSigner<TAccountOwner>;
  computeNodeInfo: Address<TAccountComputeNodeInfo>;
  systemProgram?: Address<TAccountSystemProgram>;
  nodePubkey: RegisterComputeNodeInstructionDataArgs['nodePubkey'];
};

export function getRegisterComputeNodeInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountComputeNodeInfo extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_MANAGER_PROGRAM_ADDRESS,
>(
  input: RegisterComputeNodeInput<
    TAccountPayer,
    TAccountOwner,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RegisterComputeNodeInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountComputeNodeInfo,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_MANAGER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    computeNodeInfo: { value: input.computeNodeInfo ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.computeNodeInfo),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterComputeNodeInstructionDataEncoder().encode(
      args as RegisterComputeNodeInstructionDataArgs
    ),
    programAddress,
  } as RegisterComputeNodeInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountComputeNodeInfo,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterComputeNodeInstruction<
  TProgram extends string = typeof DAC_MANAGER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    owner: TAccountMetas[1];
    computeNodeInfo: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterComputeNodeInstructionData;
};

export function parseRegisterComputeNodeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRegisterComputeNodeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      computeNodeInfo: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterComputeNodeInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
